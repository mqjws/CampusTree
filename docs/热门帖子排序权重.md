## 综合分，不是单一指标

  后端公式：

```
hot_score = view_count + comment_count * 3 + like_count * 2
```

  也就是：

  \- 浏览数：每 1 次 = 1 分
  \- 评论数：每 1 条 = 3 分
  \- 点赞数：每 1 个 = 2 分

  排序规则是：

1. 先按 hot_score 从高到低
2. 分数相同时，按 created_at 从新到旧


所以评论权重最高，其次点赞，浏览最低。